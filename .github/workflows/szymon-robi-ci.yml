name: szymon-robi-github-actions

on:
  push:
    branches:
      - '**'

jobs:
  build:
    name: Steve Jobs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello I am Steve Jobs"
